# Show issue dates

👨‍💼 Your next ticket is "Users should be see the date of each issue". Good luck!

Dates can be tricky to work with, especially in a server-side rendered application. The javascript Date API will always use the current machine's timezone, and since the user's browser is often in a different timezone than the server, we need to be careful.
